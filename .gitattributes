# Normalize line endings: LF in the repository and on checkout, on every OS.
# Keeps Windows dev consistent with Linux CI (shell scripts, etc.).
* text=auto eol=lf

# Binary assets git should not touch.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
